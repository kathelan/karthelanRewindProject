apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: kathelan-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      containers:
        - name: user-service
          image: quay.io/kathelan/kathelan-user-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8081
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: local
            - name: SOAP_CLIENT_URL
              value: http://soap-service:8080/ws
            - name: SOAP_CLIENT_USERNAME
              valueFrom:
                secretKeyRef:
                  name: soap-credentials
                  key: username
            - name: SOAP_CLIENT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: soap-credentials
                  key: password
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            tcpSocket:
              port: 8081
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            tcpSocket:
              port: 8081
            initialDelaySeconds: 15
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: kathelan-dev
spec:
  selector:
    app: user-service
  ports:
    - port: 8081
      targetPort: 8081
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: user-service
  namespace: kathelan-dev
spec:
  to:
    kind: Service
    name: user-service
  port:
    targetPort: 8081
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
